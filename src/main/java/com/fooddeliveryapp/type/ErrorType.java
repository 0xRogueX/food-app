package com.fooddeliveryapp.type;

public enum ErrorType {
    AUTHENTICATION_ERROR,
    CART_ERROR,
    ORDER_ERROR,
    RESOURCE_NOT_FOUND,
    RESOURCE_ALREADY_EXISTS,
    USER_ALREADY_EXISTS,
    VALIDATION_ERROR
}